# Base URL for the API. Same domain in production (single Vercel project).
VITE_API_URL=/api

# Google OAuth Client ID (public by design — safe to expose in the browser).
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
